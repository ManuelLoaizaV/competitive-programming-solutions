#include <bits/stdc++.h>
#define fastio ios::sync_with_stdio(0);cin.tie(0);cout.tie(0)
using namespace std;

int main() {
	fastio;
	int x, y, z;
	x = 1;
	cout << x << endl;
	fflush(stdout);
	cin >> y;
	if (y == 2) z = 3;
	else z = 2;
	cout << z << endl;
	fflush(stdout);
	return 0;
}
